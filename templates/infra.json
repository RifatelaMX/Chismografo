{
	"name": "{{name}}",
	"category": "{{category}}",
	"web": "{{web}}",
	"logo": {
		"id": "{{id_logo}}",
		"provider": "{{provider_logo}}"
	},
	"detectionRules": [
		{
			"type": "header",
			"key": "server",
			"pattern": "{{slug}}",
			"description": "Cabecera de servidor de {{name}} detectada"
		}
	]
}
