{
	"id": "{{slug}}",
	"name": "{{name}}",
	"web": "{{web}}",
	"precios": [],
	"logo": {
		"id": "{{id_logo}}",
		"provider": "{{provider_logo}}"
	},
	"detectionRules": [
		{
			"id": "{{slug}}-meta-generator",
			"type": "meta",
			"key": "generator",
			"pattern": "{{slug}}",
			"description": "Meta generador que contiene {{name}}",
			"weight": 1.0
		}
	]
}
